name: tabs
description: Tabs is a customisable Flutter tab view/carousel with an animated indicator, controller navigation, focus support, and semantics hooks.
version: 5.0.0
homepage: https://github.com/GreatPika/tabs
repository: https://github.com/GreatPika/tabs

environment:
  sdk: ^3.8.0
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
