# Tabs example

Example app for the `tabs` package.

## Running

From this directory:

```bash
flutter run -d chrome
flutter run -d macos
```

Run `flutter test` from this directory for the example smoke test.
